import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  timeout: 30_000,
  retries: 0,
  use: {
    baseURL: 'http://127.0.0.1:3000',
    trace: 'retain-on-failure',
  },
  webServer: {
    command: 'node -e "process.env.PORT=\'3000\'; process.env.HOSTNAME=\'127.0.0.1\'; process.env.AUTH_SECRET=\'playwright-test-secret-playwright-test-secret\'; process.env.E2E_AUTH_BYPASS=\'1\'; require(\'./.next/standalone/server.js\')"',
    url: 'http://127.0.0.1:3000',
    reuseExistingServer: true,
    timeout: 120_000,
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'mobile-chrome', use: { ...devices['Pixel 7'] } },
  ],
});
